## API Report File for "@hyperfixation/workflows"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { ApprovalStatus } from '@hyperfixation/db';
import { ApprovalVia } from '@hyperfixation/db';
import { Client } from 'pg';
import { DBOSClient } from '@dbos-inc/dbos-sdk';
import { NodePgDatabase } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';
import { RecordTable } from '@hyperfixation/db';
import * as schema from '@hyperfixation/db';
import { StepDatabase } from '@hyperfixation/db';
import { StepPool } from '@hyperfixation/db';
import { WorkflowQueue } from '@dbos-inc/dbos-sdk';
import { ZodType } from 'zod';

// @public
export const ABANDON_LLM_CALLS_STATEMENT: string;

// @public (undocumented)
export interface ActionChannel {
    readonly dedupes: boolean;
    // (undocumented)
    readonly name: string;
    // (undocumented)
    send(dispatch: ActionDispatch): Promise<ActionResult>;
}

// @public
export interface ActionDispatch {
    // (undocumented)
    idempotencyKey: string;
    // (undocumented)
    key: string;
    // (undocumented)
    request: unknown;
    // (undocumented)
    runId: string;
}

// @public (undocumented)
export interface ActionResult {
    // (undocumented)
    externalId?: string;
    // (undocumented)
    response?: unknown;
}

// @public (undocumented)
export const actions: {
    perform: typeof perform;
};

// @public (undocumented)
export interface ActionsPerformOptions {
    // (undocumented)
    channel: ActionChannel;
    key: string;
    // (undocumented)
    recordId?: string;
    // (undocumented)
    recordType?: string;
    // (undocumented)
    request?: unknown;
}

// @public
export class ActionUncertain extends Error {
    constructor(runId: string, key: string, actionLogId: number, taskId: number | null);
    // (undocumented)
    readonly actionLogId: number;
    // (undocumented)
    readonly key: string;
    // (undocumented)
    readonly runId: string;
    // (undocumented)
    readonly taskId: number | null;
}

// @public
export const APPROVAL_DECISIONS: readonly ["approved", "rejected", "expired", "cancelled"];

// @public (undocumented)
export class ApprovalBatchRefused extends Error {
    constructor(reasons: {
        approvalId: number;
        reason: string;
    }[]);
    // (undocumented)
    readonly reasons: {
        approvalId: number;
        reason: string;
    }[];
}

// @public (undocumented)
export interface ApprovalDecision {
    // (undocumented)
    approvalId: number;
    // (undocumented)
    decidedBy: string | null;
    // (undocumented)
    decidedVia: ApprovalVia | null;
    draft: unknown;
    // (undocumented)
    key: string;
    // (undocumented)
    status: Exclude<ApprovalStatus, "pending">;
}

// @public (undocumented)
export type ApprovalDecisionKind = (typeof APPROVAL_DECISIONS)[number];

// @public
export type ApprovalDraftSchema = ZodType;

// @public (undocumented)
export interface ApprovalNotice {
    // (undocumented)
    approvalId: number;
    // (undocumented)
    draft: unknown;
    // (undocumented)
    key: string;
    // (undocumented)
    runId: string;
    // (undocumented)
    type: string;
}

// @public (undocumented)
export class ApprovalRunMoved extends Error {
    constructor(approvalId: number);
}

// @public (undocumented)
export const approvals: {
    decide: typeof decide;
    waitForApproval: typeof waitForApproval;
};

// @public (undocumented)
export class ApprovalWriteLost extends Error {
    constructor(approvalId: number, rowCount: number);
}

// @public
export const CALLBACK_DATA_MAX_BYTES = 64;

// @public
export const CALLBACK_DATA_VERSION = "hf1";

// @public (undocumented)
export class CallbackDataTooLong extends Error {
    constructor(data: string);
}

// @public (undocumented)
export const CLIENT_POOL_SIZE = 2;

// @public (undocumented)
export interface Concluded {
    // (undocumented)
    dbosStatus: string;
    // (undocumented)
    runId: string;
    // (undocumented)
    status: "done" | "failed";
}

// @public (undocumented)
export type ControlDatabase = NodePgDatabase<typeof schema>;

// @public (undocumented)
export interface ControlPool {
    // (undocumented)
    readonly db: ControlDatabase;
    // (undocumented)
    end(): Promise<void>;
    // (undocumented)
    readonly pool: Pool;
}

// @public (undocumented)
export function currentRun(operation: string): RunContext;

// @public
export function decide(pool: Pool, dbosClient: DBOSClient, options: DecideOptions): Promise<DecideResult>;

// @public
export const DECIDE_OPERATION = "approvals.decide";

// @public (undocumented)
export interface DecidedApproval {
    // (undocumented)
    approvalId: number;
    // (undocumented)
    key: string;
    resumeWorkflowId: string;
    // (undocumented)
    runId: string;
    // (undocumented)
    status: ApprovalDecisionKind;
}

// @public (undocumented)
export interface DecideOptions {
    admin?: boolean;
    // (undocumented)
    decision: ApprovalDecisionKind;
    decisionKey: string;
    edits?: Record<number, unknown>;
    // (undocumented)
    ids: number[];
    // (undocumented)
    lockTimeout?: string;
    schemaFor?: (type: string) => ApprovalDraftSchema | undefined;
    // (undocumented)
    userId?: string | null;
    // (undocumented)
    via: ApprovalVia;
}

// @public (undocumented)
export interface DecideResult {
    batchId: string | null;
    // (undocumented)
    decided: DecidedApproval[];
    // (undocumented)
    reattempted: {
        runId: string;
        attempt: number;
        workflowId: string;
    }[];
    replayed: boolean;
}

// @public
export function decisionKeyFor(data: TelegramCallbackData): string;

// @public
export function decodeCallbackData(data: string | undefined): TelegramCallbackData | null;

// @public
export function definedFlows(): ReadonlyMap<string, Flow<never, unknown>>;

// @public
export function defineFlow<I, O>(name: string, fn: (input: I, run: RunContext) => Promise<O>, options: DefineFlowOptions): Flow<I, O>;

// @public (undocumented)
export interface DefineFlowOptions {
    // (undocumented)
    queue: QueueName;
}

// @public
export const DRAIN_TIMEOUT_MS = 60000;

// @public
export const DRIFT_STATEMENT: string;

// @public (undocumented)
export class DuplicateFlow extends Error {
    constructor(name: string);
}

// @public
export function encodeCallbackData(data: TelegramCallbackData): string;

// @public
export const EXPIRED_APPROVALS_STATEMENT: string;

// @public (undocumented)
export interface ExpiredApproval {
    // (undocumented)
    approvalId: number;
    // (undocumented)
    runId: string;
    workflowId: string;
}

// @public (undocumented)
export interface Flow<I = unknown, O = unknown> {
    // (undocumented)
    readonly name: string;
    // (undocumented)
    readonly queue: QueueName;
    readonly workflow: (args: FlowArgs<I>) => Promise<O | undefined>;
}

// @public
export interface FlowArgs<I> {
    // (undocumented)
    attempt: number;
    // (undocumented)
    input: I;
    // (undocumented)
    runId: string;
}

// @public
export function getClient(options: GetClientOptions): Promise<DBOSClient>;

// @public (undocumented)
export interface GetClientOptions {
    // (undocumented)
    appMigrationsDir?: string;
    // (undocumented)
    appName: string;
    databaseUrl: string;
    // (undocumented)
    recordTables?: readonly RecordTable[];
}

// @public
export function handleTelegramCallback(update: unknown, options: TelegramCallbackOptions): Promise<TelegramCallbackResult>;

// @public (undocumented)
export function idempotencyKey(runId: string, key: string): string;

// @public
export const LANGFUSE_ENV: readonly ["LANGFUSE_PUBLIC_KEY", "LANGFUSE_SECRET_KEY", "LANGFUSE_BASE_URL"];

// @public (undocumented)
export interface LangfuseRegistration {
    shutdown(): Promise<void>;
}

// @public (undocumented)
export const LAUNCHED_MARKER = "hf-worker: DBOS launched";

// @public
export const LAUNCHING_MARKER = "hf-worker: calling DBOS.launch";

// @public
export const LOCK_ACQUIRED_MARKER = "hf-worker: advisory lock acquired";

// @public
export function maxNonceLength(approvalId: number): number;

// @public
export const MIN_BUILD_SHA_LENGTH = 7;

// @public (undocumented)
export class MissingBuildSha extends Error {
    constructor(buildSha: string | undefined);
}

// @public (undocumented)
export class NotAWorkerProcess extends Error {
    constructor(hfProcess: string | undefined);
    // (undocumented)
    readonly hfProcess: string | undefined;
}

// @public (undocumented)
export class OutsideRun extends Error {
    constructor(operation: string);
    // (undocumented)
    readonly operation: string;
}

// @public
export const PAUSED_CONCURRENCY = 0;

// @public
export const PAUSED_QUEUES: readonly ["llm", "actions"];

// @public
export const PAUSED_RUNS_STATEMENT: string;

// @public
export function perform(ctx: StepContext, options: ActionsPerformOptions): Promise<ActionResult>;

// @public (undocumented)
export interface PeriodDrift {
    // (undocumented)
    driftUsd: string;
    // (undocumented)
    ledgerUsd: string;
    // (undocumented)
    period: string;
    // (undocumented)
    spentUsd: string;
}

// @public (undocumented)
export interface QueueConcurrency {
    applied: boolean;
    // (undocumented)
    globalConcurrency: number;
    // (undocumented)
    name: QueueName;
}

// @public (undocumented)
export interface QueueConcurrencyCorrection {
    // (undocumented)
    name: QueueName;
    // (undocumented)
    now: number;
    paused: boolean;
    was: number | null;
}

// @public (undocumented)
export type QueueName = (typeof QUEUES)[number]["name"];

// @public
export const QUEUES: readonly [{
    readonly name: "llm";
    readonly globalConcurrency: 4;
}, {
    readonly name: "actions";
    readonly globalConcurrency: 2;
}, {
    readonly name: "resolve";
    readonly globalConcurrency: 1;
}];

// @public (undocumented)
export interface Reattempted {
    // (undocumented)
    attempt: number;
    reason: "dead-version" | "cancelled" | "resumed";
    // (undocumented)
    runId: string;
    // (undocumented)
    workflowId: string;
}

// @public
export function reconcile(pool: Pool, dbosClient: DBOSClient, options: ReconcileOptions): Promise<ReconcileReport>;

// @public
export const RECONCILE_ACTION_MARKER = "hf-reconcile: run moved";

// @public
export const RECONCILE_ANOMALY_MARKER = "hf-reconcile: a running run has no workflow row";

// @public
export const RECONCILE_FAILED_MARKER = "hf-reconcile: run refused";

// @public
export const RECONCILE_INTERVAL_MS = 60000;

// @public
export const RECONCILE_PASS_MARKER = "hf-reconcile: pass";

// @public
export const RECONCILE_QUEUE_MARKER = "hf-reconcile: queue concurrency corrected";

// @public (undocumented)
export interface ReconcileAnomaly {
    enqueued: boolean;
    // (undocumented)
    runId: string;
    // (undocumented)
    workflowId: string;
}

// @public (undocumented)
export interface ReconcileFailure {
    // (undocumented)
    error: string;
    runId: string | null;
    // (undocumented)
    step: "reattempt" | "conclude" | "resume" | "anomaly" | "expire" | "queues";
}

// @public (undocumented)
export interface ReconcileOptions {
    applicationVersion: string;
    lockTimeout?: string;
}

// @public (undocumented)
export interface Reconciler {
    stop(): void;
}

// @public
export const RECONCILER_POOL_SIZE = 2;

// @public (undocumented)
export interface ReconcileReport {
    // (undocumented)
    abandonedLlmCalls: number;
    // (undocumented)
    anomalies: ReconcileAnomaly[];
    // (undocumented)
    concluded: Concluded[];
    // (undocumented)
    drift: PeriodDrift[];
    // (undocumented)
    expired: ExpiredApproval[];
    // (undocumented)
    failures: ReconcileFailure[];
    // (undocumented)
    queueConcurrency: QueueConcurrencyCorrection[];
    // (undocumented)
    reattempted: Reattempted[];
    // (undocumented)
    uncertainActions: number;
}

// @public
export function registerLangfuse(env?: NodeJS.ProcessEnv): LangfuseRegistration | undefined;

// @public
export function resetClient(): Promise<void>;

// @public (undocumented)
export interface RunContext {
    // (undocumented)
    attempt: number;
    // (undocumented)
    runId: string;
    workflowId: string;
}

// @public
export const RUNNING_RUNS_STATEMENT: string;

// @public
export function runsStart<I>(pool: Pool, dbosClient: DBOSClient, flow: Flow<I, unknown>, input: I, options?: RunsStartOptions): Promise<StartedRun>;

// @public (undocumented)
export interface RunsStartOptions {
    runId?: string;
}

// @public
export function setPausedQueueConcurrency(client: DBOSClient, paused: boolean): Promise<QueueConcurrency[]>;

// @public (undocumented)
export const SHUTDOWN_FAILED_MARKER = "hf-worker: DBOS.shutdown rejected";

// @public (undocumented)
export const SHUTDOWN_IGNORED_MARKER = "hf-worker: SIGTERM ignored, already draining";

// @public
export const SHUTDOWN_MARKER = "hf-worker: SIGTERM, calling DBOS.shutdown";

// @public
export const SHUTDOWN_WATCHDOG_MS = 75000;

// @public (undocumented)
export const START_RUN_STATEMENT: string;

// @public (undocumented)
export interface StartedRun {
    // (undocumented)
    runId: string;
    // (undocumented)
    workflowId: string;
}

// @public
export function startReconciler(pool: Pool, dbosClient: DBOSClient, options: ReconcileOptions & {
    intervalMs?: number;
}): Reconciler;

// @public
export function startWorker(options: StartWorkerOptions): Promise<Worker>;

// @public (undocumented)
export interface StartWorkerOptions {
    appMigrationsDir?: string;
    // (undocumented)
    appName: string;
    databaseUrl: string;
    recordTables?: readonly RecordTable[];
}

// @public
export function step<T>(name: string, fn: (ctx: StepContext) => Promise<T>, options?: StepOptions): Promise<T>;

// @public
export const STEP_GATE_STATEMENT: string;

// @public
export interface StepContext {
    // (undocumented)
    readonly attempt: number;
    readonly key: string;
    // (undocumented)
    readonly runId: string;
    // (undocumented)
    tx<T>(work: (db: StepDatabase) => Promise<T>): Promise<T>;
    // (undocumented)
    readonly workflowId: string;
}

// @public (undocumented)
export interface StepOptions {
    key?: string;
}

// @public
export function stubChannel(name?: string): ActionChannel;

// @public
export const SUPERSEDED_MARKER = "hf-run: superseded attempt, the flow was not run";

// @public
export class Suspend extends Error {
    constructor(runId: string, status: SuspendStatus, reason: string);
    // (undocumented)
    readonly runId: string;
    // (undocumented)
    readonly status: SuspendStatus;
}

// @public
export const SUSPEND_STATUSES: readonly ["waiting", "paused"];

// @public (undocumented)
export type SuspendStatus = (typeof SUSPEND_STATUSES)[number];

// @public
export function sweepDecisionKey(approvalId: number): string;

// @public (undocumented)
export const SYSTEM_DATABASE_POOL_SIZE = 5;

// @public (undocumented)
export const SYSTEM_DATABASE_SCHEMA = "dbos";

// @public (undocumented)
export interface TelegramCallbackData {
    // (undocumented)
    approvalId: number;
    // (undocumented)
    decision: TelegramDecision;
    nonce: string;
}

// @public
export interface TelegramCallbackFrom {
    // (undocumented)
    id: number;
    // (undocumented)
    username?: string;
}

// @public (undocumented)
export interface TelegramCallbackOptions {
    admin?: boolean;
    decide(options: DecideOptions): Promise<DecideResult>;
    secret?: {
        expected: string;
        received: string | undefined;
    };
    userFor?: (from: TelegramCallbackFrom) => string | null | undefined;
}

// @public
export type TelegramCallbackOutcome = "decided" | "refused" | "ignored";

// @public (undocumented)
export interface TelegramCallbackResult {
    // (undocumented)
    approvalId: number | null;
    callbackQueryId: string | null;
    // (undocumented)
    decision: TelegramDecision | null;
    // (undocumented)
    decisionKey: string | null;
    // (undocumented)
    outcome: TelegramCallbackOutcome;
    reason: string | null;
    replayed: boolean;
}

// @public
export type TelegramDecision = Extract<ApprovalDecisionKind, "approved" | "rejected">;

// @public
export const UNCERTAIN_ACTIONS_STATEMENT: string;

// @public (undocumented)
export class UnknownFlow extends Error {
    constructor(runId: string, flow: string);
    // (undocumented)
    readonly runId: string;
}

// @public (undocumented)
export class UnknownQueue extends Error {
    constructor(name: string, queue: string);
}

// @public
export function waitForApproval(options: WaitForApprovalOptions): Promise<ApprovalDecision>;

// @public (undocumented)
export interface WaitForApprovalOptions {
    // (undocumented)
    assigneeId?: string;
    // (undocumented)
    draft?: unknown;
    expiresInMs?: number;
    key: string;
    notify?(notice: ApprovalNotice): Promise<void>;
    // (undocumented)
    recordId?: string;
    // (undocumented)
    recordType?: string;
    type: string;
}

// @public (undocumented)
export interface Worker {
    // (undocumented)
    applicationVersion: string;
    // (undocumented)
    appName: string;
    client: DBOSClient;
    // (undocumented)
    control: ControlPool;
    // (undocumented)
    lock: WorkerLock;
    // (undocumented)
    queues: Record<QueueName, WorkflowQueue>;
    // (undocumented)
    reconciler: Reconciler;
    // (undocumented)
    steps: StepPool;
}

// @public
export const WORKER_PROCESS = "worker";

// @public (undocumented)
export interface WorkerLock {
    readonly acquiredAt: Date;
    // (undocumented)
    readonly appName: string;
    // Warning: (ae-forgotten-export) The symbol "HeldLockConnection" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly connection: HeldLockConnection;
}

// @public (undocumented)
export class WorkerLockUnavailable extends Error {
    constructor(appName: string);
    // (undocumented)
    readonly appName: string;
}

// (No @packageDocumentation comment for this package)

```
