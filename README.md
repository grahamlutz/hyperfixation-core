# hyperfixation-core

Shared packages for hyperfixation apps: `@hyperfixation/{db,core,ai,workflows,auth,admin,testing,cli}` plus a shadcn registry.

See the build plan and Phase 1 ordering docs in `../planning/`.
