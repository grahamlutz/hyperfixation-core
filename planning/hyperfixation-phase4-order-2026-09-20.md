# Hyperfixation Phase 4 — implementation order

**Date:** 2026-09-20. Derived from [hyperfixation-plan-2026-09-15.md](hyperfixation-plan-2026-09-15.md) — *Phase 4*, *The
stability contract, mechanized for npm*, *Verification*, *Done means* 3 and 4 — and v1's Phase 4 and seven contract points
(`~/Code/planning/workbench-plan-ts-2026-09-15.md` lines 126–132, 211–217; not in this repo), with
[hyperfixation-versioning-policy.md](hyperfixation-versioning-policy.md) and the tooling plan's §2 OIDC bridge as settled
premises. **The design is fixed; this document only orders it.** Same markers as Phase 3 (✅ Done / ✅ Done (deviated) /
🚧 In progress / ⬜ Not started).

**Written against** core `origin/main` `720ad4e` and template `origin/main` `7aa60e0`; every exists/does-not-exist claim
was checked in the tree or with `gh`/`npm`.

**Two concerns up front.** (1) A PR opened with the default `GITHUB_TOKEN` does not trigger `pull_request` workflows.
`core-bump.yml` (`peter-evans/create-pull-request`, no token input) and the `Version Packages` PR that `changesets/action`
opens are both such PRs, and both repos now have rulesets with required checks — so as designed today neither PR can ever
merge. Every PR-opening step below mints a token from the `hyperfixation-bot` GitHub App (private key in
`~/Code/hyperfixation-secrets/`, slug already in `HF_GITHUB_APP_SLUGS`). (2) Core has **no git tags** (local or remote), and
neither `0.1.0` nor `0.1.1` carries an attestation (`npm view … dist.attestations` is empty). The api-diff baseline is git
history, not a tag, until R2 makes the first tag.

## Status

<!-- plan-sync:start -->
<!-- Generated by `pnpm plan:sync` from merged PR bodies' `Chunk:` lines. Do not edit. -->
<!-- plan-sync:end -->

## Where Phase 4 starts from

| Piece | Exists at `720ad4e` / `7aa60e0` | Phase 4 adds |
|---|---|---|
| Fixed group, `pnpm changeset version`, CHANGELOGs, `changeset` CI job | ✅ `.changeset/config.json`, nine `CHANGELOG.md`, `changeset-check.mjs`; two changesets pending (`stdin-error-not-epipe`, `tough-pugs-listen`) → `0.1.2` is a real patch | `release.yml` |
| `release:publish` / `rehearse` / `verify` (`packages/tools`) | ✅ clone, version and `workspace:` asserts, 2FA check, per-version 200 skip, integrity check, dispatch command printed | The non-TTY publish path the workflow runs; `verify` stays |
| API Extractor gate | ✅ `pnpm turbo api-extractor` fails on any report drift (10 reports under `packages/*/etc/`) | The *removal* rule: `api-diff` + `deprecations.json` |
| Mixin snapshot | ✅ `packages/db/src/schema/records.test.ts` — "matches the committed snapshot", "nullable or defaulted, none unique" | Nothing |
| Migration allowlist | ✅ `migration-policy.ts` (app migrations, default-deny) | Nothing (core-migration additivity stays policy; see open question 5) |
| `downstream` CI job | ✅ template at `main` against the PR's tarballs via `template:check` | Every repo in `downstream.txt` |
| `downstream.txt` | ❌ (`hf new` prints the line — `checklist.ts:60`) | The file |
| `core-bump.yml` (template) | ✅ listens for `hyperfixation-core-release`; `GITHUB_TOKEN` → CI never runs on its PR; **zero** `core-bump/*` PRs ever opened | The bot token |
| `core-version.test.ts` (template) | ❌ | The test |
| `hf doctor` core-bump listing; `dependabot.yml` ignoring `@hyperfixation/*` | ✅ | Nothing |
| Runtime `deprecate()` | ❌ | Deferred (open question 3) |
| shadcn registry to Pages | `registry/registry.json` has `items: []` | Nothing (fence) |
| Secrets/environments in core; npm trusted publishers | ❌ none | User-performed (R0) |

## Corrections this pass produced

1. **The bot token everywhere a PR is opened** (concern 1). v1 said so ("opened with the bot token so its CI runs");
   Phase 3 dropped the secrets install because nothing read one. Phase 4 is where something does.
2. **`changesets/action` is v2**: inputs are `publish-script`, `github-token`, `push-git-tags`, `create-github-releases`
   (the tooling plan wrote v1's `publish:`). Its README recommends the split sub-actions under trusted publishing.
3. **Publish `pnpm -r pack` tarballs with `npm publish --provenance`**, not `pnpm -r publish`. npm ≥ 11.5.1 is the
   documented OIDC client; whether pnpm 12.4.2's `publish` mints OIDC is unverified (no release note found), and packing
   first is what `release:verify` already checks (`workspaceRangeLeftovers`).
4. **A breaking minor still opens a bump PR.** v1's "a major opens none, with a notice" is superseded by the versioning
   policy: at 0.x the minor is the break, `core-bump.yml`'s `pnpm update --latest` is the only thing that moves the
   template's caret, and the app's suite gates it.
5. **`removeIn` is a minor**, not "the next major" — two-release rule from the policy.
6. **Baseline is the last version commit**, not a tag, for `api-diff` on branches; tags start at `0.1.2`.

---

## R0 — Accounts and secrets (user; before R1 merges) — ⬜

npmjs.com, nine times: package → Settings → Publishing access → Trusted publisher → GitHub Actions, repository
`grahamlutz/hyperfixation-core`, workflow `release.yml`, environment blank; then "Require 2FA or trusted publisher,
disallow tokens". Core repo secrets `HF_BOT_APP_ID`, `HF_BOT_APP_PRIVATE_KEY` (the `.pem`). Confirm the App is installed
on core and the template with `contents: write`, `pull-requests: write`.

**Done:** `gh api repos/grahamlutz/hyperfixation-core/actions/secrets` lists both; each package page shows the publisher.

## R1 — `release.yml` (core) — ⬜

`.github/workflows/release.yml`: `on: push: branches: [main]`; `permissions: contents: write, pull-requests: write,
id-token: write`; `actions/create-github-app-token` from the two secrets; `changesets/action@v2` with `github-token: <app
token>`, `version: pnpm changeset version`, `publish-script: pnpm release:ci <version>`. `packages/tools/src/publish-ci.ts`
(`pnpm release:ci`): version and `workspace:` asserts and per-version skip from `registry.ts`, `pnpm -r build`, `pnpm -r
pack`, `npm install -g npm@11`, `npm publish <tarball> --provenance --access public` per package in `topologicalOrder`,
then `release:verify`'s checks, then tag `v<version>` pushed with the app token, then `repository_dispatch
hyperfixation-core-release {version}` to every line of `downstream.txt` (or open the bump PRs directly — open question 1).
No `NPM_TOKEN` anywhere; `release:publish` and `release:rehearse` stay until R2 proves the path (README already says so).

**Done:** `publish-ci.test.ts` against a fake registry and exec — skips a 200 version, refuses a `workspace:` leftover,
publishes in dependency order, dispatches once per line; the workflow's dry path (`--dry-run`) green on a branch push.

## R2 — First OIDC release, `0.1.2` (manual observation; after R0, R1, A1) — ⬜

Merge the `Version Packages` PR the action opens (its CI must run — that is concern 1's proof). Record here: the run
URL, `npm view @hyperfixation/core@0.1.2 dist.attestations` non-empty, `release:verify 0.1.2` clean, tag `v0.1.2`,
one dispatch per `downstream.txt` line. Then delete `publish.ts`/`rehearse.ts` and their scripts.

> **Finding — the first OIDC release went through, and the rebuild comparison was never the problem.**
> [run 35513816078](https://github.com/grahamlutz/hyperfixation-core/actions/runs/35513816078) published all nine
> packages from commit `813af20` and pushed tag `v0.1.2`. All nine carry a `slsa.dev/provenance/v1` attestation whose
> `resolvedDependencies` names `813af20` and whose workflow is `.github/workflows/release.yml`; `npm audit signatures`
> over a tree with all nine installed reports nothing invalid and nothing missing.
>
> The reported "integrity mismatch for all nine" does **not** reproduce, and there is no build non-determinism to fix.
> `npm pack`ing `@hyperfixation/eslint-config`, `db` and `core` out of a clean worktree of `813af20` on the maintainer's
> Mac produced tarballs that are **byte-identical** to the ones the ubuntu runner published — same sha512, `diff -r`
> over the extracted trees clean — and `pnpm release:verify 0.1.2` is green for all nine. The builds agree across
> machines because `--frozen-lockfile` pins the whole toolchain (`typescript` 5.9.3, `turbo`, `@types/node` are all
> caret ranges in `package.json` but exact in the committed lockfile), and nothing we emit embeds an absolute path, an
> mtime or a hostname. So no source map, `.d.ts` or `tsbuildinfo` difference exists to chase.
>
> What the episode did expose is that the comparison is the *wrong check* for a CI-published release: it can only ever
> assert that the verifier's machine agrees with the publisher's, so any disagreement — a stale toolchain, the wrong
> commit resolved (the 0.1.1 CLI false positive), a dirty store — reads as though the registry served something it
> should not have. `release:verify` now gates on the attestation instead and keeps the rebuild as a labelled
> informational check: for an attested version a byte difference is a `warnings:` line, and for `0.1.0`/`0.1.1`, which
> have no attestation, it stays the failing check because it is the only evidence there is.

## A1 — `api-diff` and `deprecations.json` (core; ∥ R1) — 🚧 In progress

`packages/tools/src/api-diff.ts` + `api-diff.test.ts`; root `deprecations.json` (`[{ package, symbol, since, removeIn }]`).
Baseline: each `packages/*/etc/*.api.md` at the merge-base with `CHANGESET_CHECK_BASE` on a PR; the last commit that
changed the fixed group's versions on `main`. A member removed or retyped must have an entry whose `since` ≤ the current
version and `removeIn` ≥ the next minor, and the PR's changeset must be `minor`; the symbol must carry `@deprecated` in
the report. Runs as a step of the `changeset` job (it already has `fetch-depth: 0`). Also: `key-derivation.test.ts`
pinning every core-shipped key (`scores.ts:186` `…:score.written:<spec>`, `tasks.ts:144` `…:task.created`, and the
ledger/action helpers') as a committed snapshot whose header says it is contract.

**Done:** fixtures — a removed export with no entry fails; with an entry but a `patch` changeset fails; an added export
passes; the key snapshot fails on a changed template string.

## B1 — `downstream.txt` and the matrix (core; after R0) — ⬜

`downstream.txt` at the root, one `owner/repo` per line, the template first. The `downstream` job becomes a matrix over
its lines (private repos checked out with the app token), each running `template:check` with `HF_TEMPLATE_DIR` at that
checkout. `release:ci` reads the same file.

**Done:** the job shows one entry per line; a deliberately renamed export fails every entry.

## B2 — The bump lands (template + cli; ∥ R1) — ⬜

Template: `core-bump.yml` mints the app token (`actions/create-github-app-token`) and passes it to
`create-pull-request` (`token:`); `tests/core-version.test.ts` asserts every installed `@hyperfixation/*` is one version
satisfying `package.json`'s range. CLI: `hf new` gains a `secrets` step — `PUT /repos/{o}/{r}/actions/secrets/{name}`
sealed with the repo's public key (`libsodium-wrappers`), from operator keys `HF_BOT_APP_ID` and
`HF_BOT_APP_PRIVATE_KEY_FILE`; the checklist line for `downstream.txt` stays. (If open question 1 is decided as
recommended, the CLI half and the token in `core-bump.yml` are dropped.)

**Done:** `core-version.test.ts` fails on a hand-edited mixed lockfile; `new.test.ts` cloud case (a) issues the two
sealed PUTs after the repo step and none on a re-run; the GitHub OpenAPI vendoring covers the public-key endpoint.

## X2 — Exit (manual; after R2, B1, B2) — ⬜

`0.1.2`'s dispatch opens `core-bump/0.1.2` on the template and on demo-app; both CIs run and are green; `hf doctor`
lists them; merge; `hf doctor` clean. Paste the PR URLs and check times here.

---

## Parallelism

| Chunk | Repo | Needs | Blocks |
|---|---|---|---|
| R0 | npmjs.com, GitHub | — (user, can start today) | R1 merge, B1 |
| R1 | core | R0 for the real run; code can land before | R2 |
| A1 | core | — | R2 (the gate must exist before the release that first relies on it) |
| B1 | core | R0 | X2 |
| B2 | template, cli | — (template half); CLI half after X1's runbook keys | X2 |
| R2 | manual | R0, R1, A1 | X2 |

R1 ∥ A1 ∥ B2 from day one; B1 after R0. Merge hot spots: `.github/workflows/ci.yml` (A1 adds a step, B1 rewrites
`downstream`) — land A1 first; `packages/tools/src/registry.ts` (R1); `packages/cli/etc/cli.api.md` (B2 — clean clone).

## Manual versus automated

| Manual (why) | Evidence |
|---|---|
| Nine trusted publishers, two secrets, App installation (npmjs/GitHub settings) | R0 done line |
| The first OIDC publish (cannot rehearse OIDC against Verdaccio) | R2's attestation and tag |
| The `Version Packages` and `core-bump` PRs' CI actually starting | run URLs in R2/X2 |
| `hf doctor` listing and clearing bump PRs | X2 |

Automated: `publish-ci` against a fake registry; `api-diff` over fixture reports; `core-version` on a mixed lockfile;
`new.test.ts` secrets step under msw; `downstream` matrix in CI.

## Exit bar, as tests

1. `pnpm -w typecheck lint test api-extractor` green (record the count); named
   `pnpm --filter @hyperfixation/tools test api-diff publish-ci`.
2. Template `pnpm test` green with `core-version.test.ts`; `pnpm --filter @hyperfixation/cli test new`.
3. `gh run list --workflow release.yml` shows the `0.1.2` run green; `npm view @hyperfixation/<each>@0.1.2
   dist.attestations` non-empty; `pnpm release:verify 0.1.2` clean.
4. `gh pr list -R grahamlutz/hyperfixation-template --search head:core-bump/0.1.2` shows a merged, green PR; same for demo-app.

## Open questions (recommendations; Graham decides)

1. **Where the bump PR is opened.** Design: `repository_dispatch` → each app's `core-bump.yml` opens it, which needs the
   bot's private key as a secret in *every* app (the App is installed on all repositories, so any app's CI compromise
   leaks write access to all). Alternative: `release:ci` clones each `downstream.txt` repo with the app token,
   `pnpm update --latest`, pushes `core-bump/<v>`, opens the PR — one secret, in core; `core-bump.yml` keeps only
   `workflow_dispatch` for a deliberate pin. **Recommend the alternative**; it removes the CLI half of B2 and the
   libsodium dependency. Blocks B2.
2. **Cut over at `0.1.2`?** Recommend yes (tooling plan said so; the two pending changesets make it a real patch).
3. **Runtime `deprecate()` + throw under `@hyperfixation/testing`.** No deprecation exists to warn about; recommend
   deferring to the first `deprecations.json` entry, and recording that here.
4. **Tag `v0.1.0`/`v0.1.1` retroactively?** Recommend `v0.1.1` only, at the commit `release:verify 0.1.1 --root` matches.
5. **A mechanical core-migration additivity check** (policy: no drop/rename against N-1)? Recommend not this phase — the
   plan lists the app allowlist, which exists; add a `core-migration-policy` test when Phase 5 has an N-1 reader.

## Follow-ups from the first-run review

Found while running a fresh clone on 2026-09-19, after Phase 2. None block a phase exit; each is
small enough to ride along with the chunk it touches. Markers as above.

- ✅ **`hf up` needed a budget `hf new` never set.** `hf up` now seeds `DEV_BUDGET_USD` ($10) when
  `HF_BOOTSTRAP_BUDGET_USD` is unset and says so; `hf bootstrap` still requires an explicit one, so a
  deploy never gets an implicit cap (core #15, template #11).
- ⬜ **No CI job runs `hf new --local && hf up`.** That bug shipped because every test set the
  budget by hand and no job ran the two commands together. Needs the built `hf` (the X1 note says the
  same about the admin form). One job that scaffolds a scratch app and runs `hf up` to the point the
  web process is listening.
- ⬜ **`hf new --local` and `hf check` don't verify the bootstrap inputs.** A blank email answer is
  accepted (`new.ts`, `promptForBootstrapEmail`) and only fails later inside `hf bootstrap`; `hf check`
  compares `.env.example` to the env and looks at migrations and E001–E006, nothing else. The cloud
  `hf new` already refuses a missing `--email`/`--budget-usd` up front — make `--local` match.
- ⬜ **The app's base budget has no command.** #28's admin action edits one `hf_budget_period` row
  (audited, passkey-gated); `hf_app_state.budget_usd`, the default each new month copies, is set once by
  `hf bootstrap` and never again. Decide whether an admin action or `hf` command should change it.
- ⬜ **Test database default is a port nothing starts.** `packages/testing/src/database.ts` defaults to
  5434, "the `hf dev` compose port"; this repo has no compose file and CI overrides it to 5432. Default to
  5432, or ship a `docker-compose.test.yml`. Also document how to get `hf` on PATH from a clone (the
  published `@hyperfixation/cli` may make this moot — check before writing it down).
- ⬜ **Docs drift.** The chunk headings above (R1, A1, B1, B2, X2), "Where Phase 4 starts from" (written
  against `720ad4e`), the Phase 3 X1 header, and the master plan's status table all predate the merged
  work. `pnpm plan:sync --write` regenerates the Status block; the rest is hand prose.
- ⬜ **shadcn registry is empty** (`registry/registry.json`, `items: []`). Fenced above; listed so it
  isn't forgotten.

## Risks

1. Concern 1 is a documented GitHub rule; if an App token also fails to trigger CI (it should not), the fallback is a
   fine-grained PAT with the same two scopes.
2. pnpm 12.4.2 OIDC support unverified — moot with `npm publish` on packed tarballs (correction 3).
3. `changesets/action` v2 may not detect a custom `publish-script`'s published packages for `push-git-tags`; R1 tags
   itself, so nothing depends on it.
4. The template's `minimumReleaseAgeExclude` must still cover `@hyperfixation/*` or the bump PR installs `0.1.1`.
5. `npm install -g npm@latest` in CI is a moving target; pin `npm@11` and bump deliberately.

**Adversary targets before R1 merges:** (a) `release.yml` on a push to `main` that carries no changesets and a version
already on the registry — it must exit 0 with nothing published and no dispatch; (b) the `Version Packages` PR under the
`changeset` job (it deletes changesets — `changeset-check.mjs` says it passes; prove it) and under `api-diff` (versions
change, reports do not); (c) the `id-token` scope — nothing else in the workflow may run with it, and a fork PR must
never reach the publish step.

**Verification, per chunk:** core `pnpm -w typecheck lint test api-extractor` from a clean clone; template
`pnpm typecheck lint test`; the named gates above; a branch push of `release.yml` with `--dry-run` before R2.
