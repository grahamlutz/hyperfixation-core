# Hyperfixation Phase 5 — implementation order

**Date:** 2026-09-20. Derived from [hyperfixation-plan-2026-09-15.md](hyperfixation-plan-2026-09-15.md) — *Phase 5*,
*Deployment shape and budgets*, *Verification*, *Done means* 2, 5, 6 (and 3's "both apps") — and v1's Phase 5
(`~/Code/planning/workbench-plan-ts-2026-09-15.md` line 134–136; not in this repo), with
[hyperfixation-versioning-policy.md](hyperfixation-versioning-policy.md) as a settled premise. **The design is fixed;
this document only orders it.** Markers: ✅ Done / ✅ Done (deviated) / 🚧 In progress / ⬜ Not started.

**Written against** core `origin/main` `e1dba5e` and template `origin/main` `819cfbb`. The "Follow-ups from X1" list this
doc folds in is in the Phase 4 doc (core #109).

**Decided (Graham, 2026-09-20):** the second app is a **permanent throwaway named `demo-two`**, kept as the second
downstream; it runs on **real provider keys** (not fixtures), under a $10 cap. The other open questions below are
recorded as recommended-by-default.

**Three concerns up front.** (1) v1 said "no core code intended"; Phase 5 carries some anyway — every item is tooling the
second `hf new` or the isolation exit depends on, none changes what an app imports, and each still passes the api-diff
and changeset gates. (2) Every X1 finding was a Coolify/host assumption. The second `hf new` is the **first run of the
X1-fixed path from nothing, with no resume cycles** — it is treated as an X-style run: read-only pre-flight, the runbook,
evidence pasted here, nothing claimed that the box did not show. (3) **Real keys change the risk.** A real provider call
has never run on the box; it spends money, and the key must reach the app without appearing in an error, log or state
file (secrets rule, core `CLAUDE.md`). X3 therefore adds a first-real-call check against the provider's own usage page.

## Status

<!-- plan-sync:start -->
<!-- Generated by `pnpm plan:sync` from merged PR bodies' `Chunk:` lines. Do not edit. -->

| Chunk | PRs | Merged | Summary |
|---|---|---|---|
| 0 | [core #51](https://github.com/grahamlutz/hyperfixation-core/pull/51), [template #28](https://github.com/grahamlutz/hyperfixation-template/pull/28) | 2026-09-19 | Version the nine packages at 0.1.0 and make each one publishable |
| A1 | [core #79](https://github.com/grahamlutz/hyperfixation-core/pull/79) | 2026-09-20 | Fail a PR that removes an exported member without the two-release deprecation |
| B1 | [core #84](https://github.com/grahamlutz/hyperfixation-core/pull/84) | 2026-09-20 | Run template:check over every downstream.txt line, behind one aggregate check |
| B2 | [template #35](https://github.com/grahamlutz/hyperfixation-template/pull/35) | 2026-09-20 | Hold the installed @hyperfixation/* set to one version, and stop opening bump PRs twice |
| C1 | [core #113](https://github.com/grahamlutz/hyperfixation-core/pull/113) | 2026-09-20 | Print the CLI's version, and name the directory that is in hf new's way |
| C2 | [core #114](https://github.com/grahamlutz/hyperfixation-core/pull/114) | 2026-09-20 | Let a running app's own churn be drift rather than a failed restore check |
| C3 | [core #111](https://github.com/grahamlutz/hyperfixation-core/pull/111) | 2026-09-20 | Let a const's literal set grow but not shrink, so a removed hf command fails the gate |
| D1 | [template #30](https://github.com/grahamlutz/hyperfixation-template/pull/30), [core #115](https://github.com/grahamlutz/hyperfixation-core/pull/115) | 2026-09-20 | Build the image in CI and make it name its own commit (D1) |
| D2 | [template #29](https://github.com/grahamlutz/hyperfixation-template/pull/29) | 2026-09-19 | Initialise Sentry in the web and the worker, errors only |
| D3 | [template #31](https://github.com/grahamlutz/hyperfixation-template/pull/31) | 2026-09-20 | Bring the prod compose stack up in CI and assert it (D3) |
| D4 | [template #32](https://github.com/grahamlutz/hyperfixation-template/pull/32) | 2026-09-20 | Assert a redeploy across a step change in CI (D4) |
| docs | [core #76](https://github.com/grahamlutz/hyperfixation-core/pull/76), [core #77](https://github.com/grahamlutz/hyperfixation-core/pull/77), [core #107](https://github.com/grahamlutz/hyperfixation-core/pull/107), [core #108](https://github.com/grahamlutz/hyperfixation-core/pull/108), [core #109](https://github.com/grahamlutz/hyperfixation-core/pull/109), [core #110](https://github.com/grahamlutz/hyperfixation-core/pull/110) | 2026-09-20 | Record 0.1.1, the publishing lessons and the X1 fixes in the Phase 3 doc |
| E1 | [core #52](https://github.com/grahamlutz/hyperfixation-core/pull/52) | 2026-09-19 | Give the cloud path its config, state cache and five validated clients |
| E2 | [core #54](https://github.com/grahamlutz/hyperfixation-core/pull/54) | 2026-09-19 | Give the cloud path a runner, two database transports and its provisioning |
| E3 | [core #58](https://github.com/grahamlutz/hyperfixation-core/pull/58), [core #61](https://github.com/grahamlutz/hyperfixation-core/pull/61), [core #62](https://github.com/grahamlutz/hyperfixation-core/pull/62) | 2026-09-20 | Give the cloud hf new its template fetch, env overlay and step runner |
| E4 | [core #55](https://github.com/grahamlutz/hyperfixation-core/pull/55) | 2026-09-20 | Give the cloud path hf doctor, one line per finding |
| E5 | [core #56](https://github.com/grahamlutz/hyperfixation-core/pull/56) | 2026-09-20 | Give hf a restore-check that proves a backup restores |
| M1 | [core #112](https://github.com/grahamlutz/hyperfixation-core/pull/112) | 2026-09-20 | Fail a core migration that is not additive against the last release |
| misc | [core #60](https://github.com/grahamlutz/hyperfixation-core/pull/60), [core #57](https://github.com/grahamlutz/hyperfixation-core/pull/57), [core #75](https://github.com/grahamlutz/hyperfixation-core/pull/75), [core #78](https://github.com/grahamlutz/hyperfixation-core/pull/78), [template #36](https://github.com/grahamlutz/hyperfixation-template/pull/36), [core #85](https://github.com/grahamlutz/hyperfixation-core/pull/85), [core #92](https://github.com/grahamlutz/hyperfixation-core/pull/92), [core #96](https://github.com/grahamlutz/hyperfixation-core/pull/96), [template #38](https://github.com/grahamlutz/hyperfixation-template/pull/38), [core #103](https://github.com/grahamlutz/hyperfixation-core/pull/103), [template #42](https://github.com/grahamlutz/hyperfixation-template/pull/42), [core #105](https://github.com/grahamlutz/hyperfixation-core/pull/105), [template #43](https://github.com/grahamlutz/hyperfixation-template/pull/43), [template #44](https://github.com/grahamlutz/hyperfixation-template/pull/44), [template #47](https://github.com/grahamlutz/hyperfixation-template/pull/47) | 2026-09-20 | Stop a child that exits before reading its stdin from failing the run with EPIPE |
| R1 | [core #80](https://github.com/grahamlutz/hyperfixation-core/pull/80), [core #81](https://github.com/grahamlutz/hyperfixation-core/pull/81), [core #82](https://github.com/grahamlutz/hyperfixation-core/pull/82) | 2026-09-20 | Release on a push to main: OIDC publish, our own tag, and the downstream bump PRs |
| R2 | [core #90](https://github.com/grahamlutz/hyperfixation-core/pull/90) | 2026-09-20 | Verify a release against its provenance attestation, not a rebuild of it |
| S1 | [core #116](https://github.com/grahamlutz/hyperfixation-core/pull/116) | 2026-09-20 | Prove in CI that the packed CLI can still scaffold and start an app |
| tooling | [core #68](https://github.com/grahamlutz/hyperfixation-core/pull/68), [core #71](https://github.com/grahamlutz/hyperfixation-core/pull/71) | 2026-09-20 | Generate the phase-order status table from merged PR bodies |
| W1 | [core #117](https://github.com/grahamlutz/hyperfixation-core/pull/117) | 2026-09-20 | Give demo-app core-bump PRs, and fail when release.yml's token list drifts from downstream.txt |
| X1 | [core #73](https://github.com/grahamlutz/hyperfixation-core/pull/73) | 2026-09-20 | Add the X1 runbook for the first real-box deployment |
| X1-fixes | [core #74](https://github.com/grahamlutz/hyperfixation-core/pull/74), [template #33](https://github.com/grahamlutz/hyperfixation-template/pull/33), [template #34](https://github.com/grahamlutz/hyperfixation-template/pull/34), [core #87](https://github.com/grahamlutz/hyperfixation-core/pull/87), [core #89](https://github.com/grahamlutz/hyperfixation-core/pull/89), [core #88](https://github.com/grahamlutz/hyperfixation-core/pull/88), [core #91](https://github.com/grahamlutz/hyperfixation-core/pull/91), [core #94](https://github.com/grahamlutz/hyperfixation-core/pull/94), [core #97](https://github.com/grahamlutz/hyperfixation-core/pull/97), [core #99](https://github.com/grahamlutz/hyperfixation-core/pull/99), [core #100](https://github.com/grahamlutz/hyperfixation-core/pull/100), [core #98](https://github.com/grahamlutz/hyperfixation-core/pull/98) | 2026-09-20 | Fix two X1 blockers: hf doctor on an older /api/status, and hf new's config check |
<!-- plan-sync:end -->

## Where Phase 5 starts from

| Piece | Exists at `e1dba5e` / `819cfbb` | Phase 5 adds |
|---|---|---|
| `demo-app` live at `https://demo-app.hyperfixation.ai`, core `0.1.6`, fixtures | ✅ (Phase 3 X1) | A second app beside it |
| Pause/resume per app | ✅ `POST /api/status/pause\|resume` (write token); `hf_app_state.paused` in the app's own DB | Nothing — measured, not built |
| Worker lock per app | ✅ `worker-lock.ts` `pg_try_advisory_lock(hashtext('hf-worker:' \|\| app))` in the app's own DB | `hf doctor` `lock` line |
| `hf doctor` | ✅ every app in the state dir: `status`, `runs`, `llm`, `version`, `budget`, `E006`, `restore-check`, `core-bump` | Box-wide `connections`; per-app `lock` |
| `downstream.txt` + release-opened bump PRs | ✅ template only; `demo-app` has never had one | Both app lines |
| `core-version.test.ts`, `prod-redeploy.e2e.ts` (template) | ✅ | Nothing |
| `hf restore-check` | ✅ exact counts only (X1: 9/24 mismatch on a 1.3 h dump) | Drift tolerance |
| api-diff over `COMMANDS` | ✅ additions pass; **removals pass too** | Superset rule |
| `hf --version`; `hf new` in an occupied directory | ❌ / adopt refused (core #97), no path named | Both |
| CI job running `hf new --local && hf up` | ❌ | The job |
| Core-migration additivity test (Phase 4 OQ5) | ❌ | The test |
| A real provider call on the box | ❌ (fixtures only) | X3's first-real-call check |

## Corrections this pass produced

1. **Isolation is by construction, so the exit measures it.** Pause flag, reconciler and advisory lock all live in the
   app's own database; nothing in core is shared between two apps except the Postgres server and the box.
2. **"Connection totals within budget" has two ceilings**: 25 per app role, and the server's `max_connections`, which the
   plan never read (Postgres defaults to 100; the plan's arithmetic assumed ~200 for four apps). P0 reads it.
3. **Done-means 3's "both apps" finishes here, not in Phase 4** — X2 stopped at the template.
4. **Langfuse is one shared project** (Hobby plan; Phase 3 finding). "Nothing about the first app changes" tolerates that;
   whether traces name their app is checked in X3.
5. **The second app is the first `hf new` on a box that already holds one** — name-prefix collisions (`hf_demo_app` vs
   `hf_demo_two`, backup-schedule matching by `databases_to_backup`, container discovery) are the new class of bug.

---

## P0 — Pre-flight and decisions (user; read-only; before X3) — ✅ Done

On the box (over the X1 tunnel as `postgres`): `SHOW max_connections;`, `SELECT count(*) FROM pg_stat_activity;`,
`free -m`, `docker stats --no-stream` (headroom ≥ 1.6 GB beyond demo-app's three containers). Coolify:
`GET /s3-storages` still one `is_usable`; `GET /projects` has no project named `demo-two`. Cloudflare: no `A` record at
`demo-two.hyperfixation.ai`. GitHub: both App installations still on **All repositories**; `gh repo view
grahamlutz/demo-two` is 404. `hf doctor` for `demo-app` clean except `llm` WARN. **Provider key:** put
`HF_ANTHROPIC_API_KEY` (and `HF_OPENAI_API_KEY` if the flows use it) into `~/.config/hf/config.json` with
`~/.config/hf/setkey.sh` from the clipboard, never typed or printed; confirm the provider account's spend limit is at or
below the $10 cap before X3.

**Done means:** the numbers pasted here; `max_connections − current ≥ 30`; the key is in the config and the provider's
limit is set.

> **Read on the box, 2026-09-20.** `max_connections` is **200** with **10** backends in use. The box has **7746 MB**
> total and **3946 MB** available; Metabase is the largest container at ~1.2 GiB. Coolify holds two projects,
> `shared-infra` and `demo-app`, and one usable S3 storage, `hetzner-backups`. No Cloudflare record, GitHub repository
> or database exists for `demo-two`. The provider key went into the operator config through `setkey` and was never
> printed.
>
> **DECISION (Graham, 2026-09-20).** The Anthropic account limit is **$50** — accepted instead of creating a dedicated
> $10 workspace — so risk 7's "hard limit at or below the cap" is knowingly relaxed. The app's own $10 ledger cap is
> the first line.

## W1 — `downstream.txt` gains `grahamlutz/demo-app` (core; now, before the pending `Version Packages` merges) — ✅ Done (deviated — core #117)

One line. The `downstream-matrix` job then runs `template:check` against `demo-app` on every core PR, and the next
release opens `core-bump/<version>` there. Close or merge demo-app's dependabot `typescript@6` PR first, so the first
matrix entry starts green.

**Done means:** the PR's own `downstream (grahamlutz/demo-app)` check exists and is green.

> **Built, 2026-09-20 (core #117).** Not one line but two: the plan had not foreseen that `release.yml` keeps its own
> repository list for the app token, so `downstream.txt` alone would have left the release job unable to open a bump PR
> on `demo-app`. `demo-app` went into both, and a guard test `release-repositories.test.ts` now fails when the two
> lists drift. First proof: release **0.1.7** (published 2026-09-20, all nine attested) opened `core-bump/0.1.7` on the
> template (#46) and on `demo-app` (#11) by itself; both merged when green, and `hf deploy demo-app` put `0.1.7` on the
> box — Done-means 3 for two apps.

## C1 — `hf --version` and the occupied-directory message (cli; ∥ everything) — ✅ Done (deviated — core #113)

`hf --version` prints the package version; `hf new <name>` in a directory that already holds an app (or a
`.<name>.hf-new` scratch) refuses with the existing path in the message. Changeset `patch`.

**Done means:** `cli.test.ts` — `--version` equals `package.json`; `new.test.ts` — the refusal names the path.

> **Built, 2026-09-20 (core #113).** `hf --version`, `-v` and `hf version` all print the package version, and every
> occupied-directory message names the path that is in the way. Deviation: the cloud template step no longer `rm -rf`s a
> scratch directory blindly — a `templateFetchStartedAt` state key gates the cleanup, so only a scratch this run created
> is removed.

## C2 — `hf restore-check` tolerates live drift (cli; ∥) — ✅ Done (deviated — core #114)

A committed list of append-only `hf_*` tables; for those `restored ≤ live` is verdict `ok (drift +N)`, `restored > live`
stays `mismatch`; every other table stays exact. The dump's age is printed beside the verdict and a dump older than 24 h
is a `WARN`. `--strict` restores exact matching. `patch`.

**Done means:** `restore-check.test.ts` — a fresher live count on an append-only table passes; a smaller one fails; a
non-listed table with any difference fails; age printed.

> **Built, 2026-09-20 (core #114).** `APPEND_ONLY_TABLES` holds eight tables, each verified against the schema and the
> code that writes it; a listed table with a fresher live count reads `ok (drift +N)`, `--strict` drops the allowance,
> and a dump older than 24 h is a `WARN` with exit 1. Deviation: the drift is an optional field on `RestoreCheckRow`
> rather than a fifth verdict, so nothing reading the verdicts had to learn a new one.

## C3 — api-diff catches a removed CLI command (tools; ∥) — ✅ Done (core #111)

The `COMMANDS`/`USAGE` excuse becomes "the new tuple is a superset of the old"; a removed command needs a
`deprecations.json` entry like any export.

**Done means:** `api-diff.test.ts` fixture — added command passes, removed command fails, removed with entry passes.

> **Built, 2026-09-20 (core #111).** The rule is general: a const's string-literal set may grow but not shrink, and a
> removed command needs a `deprecations.json` row keyed `COMMANDS.<name>` like any other export. `USAGE` stays exempt.

## D1 — `hf doctor` for a box with several apps (cli; ∥; wanted before X4) — ✅ Done (core #115)

Two lines per run over the tunnel E006 already opens: **`connections`** (box-wide) — `SUM` of `pg_stat_activity` over
every `hf_*` role against `max_connections` (WARN above 80 %), and per app against its role limit; **`lock`** (per app) —
exactly one advisory lock in the app's database whose key is `hashtext('hf-worker:' || app)`, FAIL on zero (no worker)
or two. `patch`.

**Done means:** `doctor.test.ts` — one app at 7/25 on a 100-slot server is OK; 85/100 warns; zero locks fails; two apps'
findings are independent.

> **Built, 2026-09-20 (core #115).** `connections` reads box-wide against `max_connections` and each app's role against
> its limit of 25; `lock` asserts exactly one advisory lock in the app's database under
> `hashtext('hf-worker:' || app)`, with the key split verified against a real Postgres rather than recomputed in
> TypeScript.

## S1 — CI scaffolds an app from the packed CLI (core; ∥) — ✅ Done (deviated — core #116)

A `scaffold` job in `ci.yml`: pack the nine packages (as `template:check` does), `npm i -g` the CLI tarball, `hf new
--local scratch --email … --budget-usd 10`, `hf up` until the web process answers `/api/status` 401, then the app's
`pnpm test`. Folds the Phase 4 follow-up "`hf new --local`/`hf check` don't verify bootstrap inputs": `--local` refuses a
blank email or budget up front like the cloud path. Not a required check until it has been green for a week.

**Done means:** the job is green on a PR; a PR that blanks the template's `.hyperfixation-template` marker fails it.

> **Built, 2026-09-20 (core #116).** The `scaffold` job runs in **3m02s** and is not a required check yet. Three
> deviations: no postgres service, because the scratch app never reaches a database; `pnpm install
> --no-frozen-lockfile`, because pnpm 12 ignores `npm_config_frozen_lockfile`; and the global install passes `--before`
> 24 h back, after a live half-publish of `@peculiar/asn1-*` broke `npm i -g` outright. The folded Phase 4 follow-up
> landed with it: `hf new --local` now refuses a missing `--budget-usd` or `--email` up front, as the cloud path does.

## M1 — Core-migration additivity test (db; ∥; may slip to Phase 6) — ✅ Done (deviated — core #112)

`packages/db/src/migration-additivity.test.ts`: every `migrations/*.sql` after the last released one contains no `DROP
TABLE|COLUMN`, `RENAME`, `ALTER … TYPE` without an allowlist entry naming the release that added the replacement (the
policy's two-release rule, mechanised). Closes Phase 4 OQ5.

**Done means:** a fixture migration with a bare `DROP COLUMN` fails; an allowlisted one passes.

> **Built, 2026-09-20 (core #112).** Deviation: the journal baseline grows with every migration PR, so it cannot mark a
> release boundary — the check therefore covers **every** committed migration rather than only those after the last
> release, with `0007`'s `DROP INDEX` allowlisted.

## T1 — Two template fixes Phase 5 needed (template; not foreseen) — ✅ Done (template #44, #47)

Neither is in the plan; both came out of Phase 5's own work.

> **Built, 2026-09-20.** **#44** — a generated app inherits the template's CI verbatim, and the `image` job's
> template-only e2e steps cannot run there; they are now gated on the `.hyperfixation-template` marker, and dependabot
> ignores the `typescript` and node Docker majors. Found by reading `demo-app`'s own CI before W1 added it to the
> matrix: it was red on `image`, which would have made core's aggregate red on every PR. **#47** — the production
> compose published host port 3000, so a second app on the box could not start; the service now only `expose`s it, the
> e2e gets a test-only overlay, and a guard test `compose-ports.test.ts` keeps the port from coming back.

## X3 — The second app, `demo-two` (user; after P0, W1; C1/C2/D1 published first if ready) — 🚧 In progress

`npm i -g @hyperfixation/cli@latest`; `hf new demo-two --budget-usd 10 --email …` from `~/Code`, runbook §3. Expected: exit
0 **with no resume**; each fix cycle, if any, is a row in the findings table here as in X1. Then `hf doctor` (both apps),
`hf restore-check demo-two` right after a Coolify "Backup Now" and, once C2 is in, again an hour later. Verify demo-app:
`/api/status` unchanged (`applicationVersion`, `coreVersion`, run counts); its repo has no new commit; its Coolify
project untouched; its backup schedule still names only `hf_demo_app`. Langfuse: one trace from each app, and whether the
app is identifiable on it. **First real provider call (new):** start one draft run on `demo-two`; `/api/status` `llm.mode`
is `live`; the `hf_llm_call` row's model and cost are compared with the provider's own usage page for that call (Phase 2's
manual "cost table vs a real bill line"); the key appears nowhere in the worker log, the state file or Sentry. Then **W2**:
one core PR adding `grahamlutz/demo-two` to `downstream.txt`.

**Done means:** two `hf new` runs on one box with one core change between them (the `downstream.txt` line) — Done-means 2;
one real call reconciled to a real usage line.

> **The run, 2026-09-20.** The first `hf new demo-two` went through steps 1–9 with **no fix cycles** — the S3 backup
> registration worked, 11 env vars including the Anthropic key reached the app, migrations ran and the bootstrap admin
> was created — and failed only at deploy.

| Finding | Fix | PR |
|---|---|---|
| Deploy failed: `Bind for :::3000 … port is already allocated`. A published host port is one per box, and `demo-app` had it | the prod compose `expose`s the port instead of publishing it; test-only overlay for the e2e; `compose-ports.test.ts` | template #47 ✅ |
| `hf_budget_period.spent_usd` was `numeric(12,4)` while `hf_llm_call.cost_usd` is `numeric(12,6)`, so the period total rounded on every settle: `/api/status` read `spentUsd 0.0017` against `ledgerUsd 0.001693`, drift `0.000007`, health `degraded`. Measured: 200 settles of ~$0.0008 end $0.0007 off the ledger, and a $0.0049 cap admits six calls where five fit | migration `0009` widens `spent_usd` to the ledger's scale; a bounded backfill repairs only rows whose gap is explainable as rounding; `packages/ai/src/budget-scale.test.ts` | core #118 🚧 in progress |
| core #118's `downstream` and `scaffold` jobs fail on the template's `tests/admin-budget.test.ts`, which pins `spent_usd` at four decimals | the assertions read the value at whatever scale the ledger stores | template #48 ✅ |

> **After #47.** `demo-two` serves `https://demo-two.hyperfixation.ai` on `3bf4cef`, core `0.1.7`, `llm.mode` `live`;
> Coolify routes to it with only `expose`, so that assumption is no longer unverified. `hf doctor` shows both apps'
> `lock` and `connections` OK (`demo_two` 3/25, box 11/200). `demo-app` is untouched: same commit, 131 runs done, 0
> failed. **First real provider calls:** two `claude-haiku-4-5` calls — 488 in / 70 out → $0.000838 and 485 in / 74 out
> → $0.000855, matching the list price of $1/$5 per MTok exactly; ledger $0.001693.
>
> **Pending:** Graham's comparison with the Anthropic console usage page; **W2** — one core PR adding
> `grahamlutz/demo-two` to `downstream.txt` **and** to `release.yml`'s repository list (W1's lesson); `hf restore-check
> demo-two` after a backup; whether Langfuse identifies the app on a trace (open question 3).

> **Finding (X3, 2026-09-20): the first two real calls put `/api/status` into `degraded` on a drift that was only a
> column's scale.** Haiku 4.5 billed $0.000838 and $0.000855 — exactly list price for 488/70 and 485/74 tokens — but
> `hf_budget_period.spent_usd` was `numeric(12,4)` against `hf_llm_call.cost_usd`'s `numeric(12,6)`, so the period read
> `spentUsd "0.0017"`, `ledgerUsd "0.001693"`, `driftUsd "0.000007"`, and `hf doctor` WARNed `drift`. Fixtures cost 0, so
> nothing before a real call could show it. The settle is `spent_usd = spent_usd + cost_usd`, so the rounding was of the
> running total and grew per call: 200 settles of those two prices land on `0.1700` against a ledger of `0.169300`, and a
> period capped at `$0.004900` admits six calls of $0.000838 where five fit. Fixed in core 0.1.8 — migration
> `0009_budget_spent_scale` widens the column to `numeric(12,6)` and repairs the rounding it had already accumulated;
> drift is now subtracted in Postgres at that scale, so a settled period reads `0.000000` and not a tolerance.

## X4 — Isolation exit (user; after X3; D1 makes it two commands) — ⬜

With both apps' schedules ticking: `POST /api/status/pause` on `demo-two` (write token from its state file). Evidence:
its `/api/status` `paused: true`, queues 0, its `running` runs reach `paused`; `demo-app`'s next tick still starts runs
and its `reconcile` keeps stamping (`/api/status` `lastReconcile` advances); `hf doctor` `lock` OK on both, keys differ
per database. `hf deploy demo-two` while paused, then `resume`: its paused runs complete; `hf_llm_call` row count per run
unchanged, zero `possible_double_charge` — Done-means 5. **Budget lever (new, because keys are real):** lower
`demo-two`'s current period cap to a small value with the admin form and confirm the next LLM call is refused and no
provider call is made; restore the cap. Then the 30-minute soak over **both** roles: peak per role < 24 and total <
`max_connections − 10`. Close X1's leftovers here: a phone passkey enrolment on either app (`hf_passkey` = 1, `decided_by`
on a phone-approved batch), and — if a run is ever observed `running` mid-step — `docker kill -s TERM` per runbook §4.6;
otherwise it stays covered by core's redeploy cases and is said so. Calendar: 2026-10-01 first-of-month row on both apps.

**Done means:** the table above filled with numbers.

> **Unchanged, waiting on X3** — but two of its readings are now commands rather than `psql`: D1's `lock` and
> `connections` lines (core #115) and C2's drift tolerance (core #114).

> **Finding, 2026-09-20 — the phone passkey enrolment above was reachable without a phone (HIGH).** A security
> review of the enrolment flow found that `upgradeSessionFactor` promoted on `token = $1 AND factor = 'code'` and
> nothing else. The WebAuthn ceremony is entirely client-side, so the template's `promoteSession()` server action —
> guarded only by `requireSession({ factor: 'code' })` — could be invoked directly by anyone holding a code-factor
> session: read an admin's inbox, sign in with the emailed code, call the action, hold `factor = 'passkey'` and the
> whole of `/admin/*`. The second half was that a code session could *enrol* on an account that already had a
> passkey, so the attacker could register their own authenticator and then promote legitimately.
>
> **The first fix was itself broken, two ways, both proved with runnable code against a real Postgres.**
> *(1, CRITICAL)* Its `before` hook matched only the two registration paths, but the plugin also exposes
> `/passkey/list-user-passkeys`, `/passkey/delete-passkey` and `/passkey/update-passkey`, each guarded on a session
> and — on two of them — ownership, never a factor. So the chain was: code session → list the victim's passkey ids →
> delete them → the enrolment gate now reads "this user has none, let them bootstrap" → enrol the attacker's own
> authenticator → promote → `/admin/*`. The gate undone by the endpoint next to it. *(2, HIGH)* Its promotion
> predicate, `EXISTS (… p.created_at >= hf_session.created_at)`, bound the promotion to a **time** rather than to a
> session: the victim holds an old authenticator, the attacker's inbox-only session sits idle, and an hour later the
> victim legitimately adds a second device from their own passkey session — at which point a passkey newer than the
> attacker's session exists and the untouched session promotes itself.
>
> **The design that replaced it.** *(A)* An **inversion**: every `/passkey/*` endpoint except the two unauthenticated
> sign-in paths (`generate-authenticate-options`, `verify-authentication`) is answered for by factor, so an endpoint a
> plugin upgrade adds is refused before anyone reads its release notes. A `factor = 'code'` session may reach only
> `generate-register-options` and `verify-registration`, and only while its user holds **zero** passkeys; everything
> else 404s. A `factor = 'passkey'` session may drive all of them. *(B)* A row in the new
> `hf_session_passkey_enrolment` table (core migration `0010`, a `CREATE TABLE` and nothing else; `session_id` primary
> key, cascading from `hf_session`), written by a `hooks.after` on `/passkey/verify-registration` for **the calling
> session** and only when the plugin returned a verified registration — better-auth runs after-hooks over a thrown
> `APIError` too, so the success check is the difference between a ceremony and a POST of junk. A table rather than a
> column on `hf_session` because `hfSession`'s printed type is public API and a column reads as a *retyped* member to
> `api-diff`, where a new table is an added export (#131). The promotion is then one statement:
> `UPDATE hf_session SET factor = 'passkey', updated_at = now() WHERE token = $1 AND factor = 'code' AND expires_at >
> now() AND EXISTS (SELECT 1 FROM hf_session_passkey_enrolment e WHERE e.session_id = hf_session.id) AND EXISTS
> (SELECT 1 FROM hf_passkey p WHERE p.user_id = hf_session.user_id)` — no timestamp comparison anywhere, both
> `EXISTS` clauses time-free.
>
> The code factor stays the bootstrap for a *first* passkey, which is what keeps X4's enrolment and the admin
> `resetSecondFactor` recovery path working. **Upgrade safety:** a session minted by passkey sign-in needs no stamp
> (it is already `passkey`), and every code session already in a live database has no enrolment row — it
> simply cannot promote until a fresh enrolment, which is the right answer and needs no backfill. The X4 reading is
> unchanged — `hf_passkey` = 1 after a real phone enrolment — but it now proves something it did not before.

## X5 — A release reaches both apps (user; after W2; the next `Version Packages` PR) — 🚧 In progress

Merge it. Evidence: three `core-bump/<v>` PRs (template, `demo-app`, `demo-two`), each CI green, each merged; `hf deploy`
both apps; `hf doctor` clean; `core-version.test.ts` green in both app repos. Done-means 3 with two apps.

> **Half proven, 2026-09-20.** `0.1.7` opened bump PRs on the template (#46) and on `demo-app` (#11) with nothing done
> by hand, both merged when green, and `hf deploy demo-app` published it (see W1). `demo-two` becomes the third entry
> once W2 lands; the release after that is the whole of X5.

---

## Parallelism

| Chunk | Repo | Needs | Blocks |
|---|---|---|---|
| P0 | box, accounts | provider key + limit | X3 |
| W1 | core | — (today) | X5's demo-app bump; the pending `Version Packages` should merge after it |
| C1, C2, C3, D1, S1, M1 | core | — | X3 wants C1/C2/D1 *published*; X4 wants D1 |
| T1 (added) | template | — (#44 before W1, #47 during X3) | W1's matrix; X3's deploy |
| X3 (+W2) | laptop, box | P0, W1 | X4, X5 |
| X4 | box | X3 | exit |
| X5 | GitHub | W2, a release after it | exit |

C1 ∥ C2 ∥ C3 ∥ D1 ∥ S1 ∥ M1 from day one. Hot spots: `packages/cli/etc/cli.api.md` (C1, C2, D1 — clean clone),
`packages/cli/src/cli.ts` (C1), `.github/workflows/ci.yml` (S1 only). Publish order: W1 → merge the pending `Version
Packages` → C1/C2/D1 land → their `Version Packages` → X3 on that CLI. If that slips, X3 runs on the current CLI and D1's
two lines are done with `psql` by hand.

## Manual versus automated

| Manual (why) | Evidence |
|---|---|
| `hf new` on a box that already holds an app (every X1 finding was a box assumption) | X3 findings table, exit code |
| First real provider call and its bill line (real money, real provider) | X3 reconciliation |
| Pause/resume isolation across two live workers (two real processes, one server) | X4 table |
| Two-role soak; `max_connections` | X4 numbers |
| Bump PRs on a private app's CI; `hf deploy` after a merge | X5 URLs |
| Phone passkey; first-of-month | X4 |

Automated: `doctor`, `restore-check`, `new`, `cli` tests; `api-diff` fixtures; the `scaffold` job; the migration
additivity test; the `downstream` matrix with three entries.

## Exit bar, as tests

1. Core `pnpm -w typecheck lint test api-extractor` green from a clean clone (record the count); named `pnpm --filter
   @hyperfixation/cli test doctor restore-check new cli`, `pnpm --filter @hyperfixation/tools test api-diff`, `pnpm
   --filter @hyperfixation/db test migration-additivity`.
2. Core CI on a PR shows `downstream (…)` × 3 and `scaffold` green.
3. `hf doctor` exit 0 with two apps, `lock` OK on each, `connections` OK box-wide.
4. X3, X4, X5 tables filled; `gh pr list -R grahamlutz/demo-two --search head:core-bump/` shows a merged green PR.

## Open questions

Decided by Graham on 2026-09-20: **1** (second app: permanent `demo-two`, kept as the second downstream) and **2**
(provider keys: real keys on the second app, $10 cap).

3. **Langfuse per app.** Recommend keeping the shared project; if X3 shows traces don't name the app, a Phase 6
   follow-up sets the app name as the trace `environment`/`release`.
4. **`hf pause|resume <name>`.** Recommend not this phase — `curl` with the state-file token is two lines and adds no API
   surface; revisit when a third app exists.
5. **M1 now or Phase 6.** Recommend now (small, closes Phase 4 OQ5); it may slip without touching the exit.
6. **When an app's CI is red, core's `downstream` aggregate is red.** That is Done-means 4 working; recommend accepting it
   and keeping app repos on the template's shape.

## Risks

1. **`max_connections`** may be Postgres's default 100; two apps fit, four do not. P0 reads it; raising it is a Coolify
   Postgres setting and a restart — not in this phase.
2. **Name-prefix collisions** in the backup step's `databases_to_backup` match and the tunnel's container discovery:
   verified for one app only. Adversary target (a).
3. **Downstream matrix cost** triples per core PR; if it exceeds ~10 min, cache the template's install.
4. **Drift tolerance can hide a lossy dump** if a listed table is not truly append-only. The list is explicit and
   `hf_run`/`hf_llm_call`/`hf_action_log`/`hf_approval`/`hf_activity`/`hf_audit` only; anything with deletes stays exact.
5. **The bump PR on a private repo** needs the App installed there — verified only by hand (personal account).
6. **A release between W1 and W2** opens two bump PRs, not three; that is expected, not a defect.
7. **Real keys.** A leaked provider key is real money. Keys enter only through the operator config (0600) and Coolify's
   environment; the template's scrubber covers logs and Sentry; the provider account carries a hard spend limit at or below
   the cap. If a key ever appears in a log or transcript, rotate it before anything else. **Knowingly relaxed (P0's
   DECISION):** the Anthropic account limit is $50, so the app's own $10 ledger cap is the first line, not the
   account's.

**Adversary targets before X3:** (a) everything two apps on one box share — Coolify project/app naming, compose service and
network names, the backup-schedule match, `HF_DB_CONTAINER` discovery, the S3 storage, the Cloudflare sender, the state
directory — and any of them keyed by a prefix rather than an exact name; (b) C2's append-only list — a table where a
restore losing rows reads as "drift"; (c) `publish-ci` opening three PRs when one repo refuses (must open the others and
report the one); (d) the first real provider call — a key present in any error path, log line, Sentry event or state
file.

**Verification, per chunk:** core `pnpm -w typecheck lint test api-extractor` from a clean clone; the named gates above;
template `pnpm typecheck lint test`; for anything touching the box, the P0 pre-flight and the X3/X4 tables.
